exports.forData = async function (messages) {
    messages.forEach( (message) => {
        // match message.source to map
        // fetch required mapping
        // create primary and metadata objects
        // push primary and metadata objects to disc
        // return
    });
}

exports.forLog = async function(log) {
    // send log to log queue
}